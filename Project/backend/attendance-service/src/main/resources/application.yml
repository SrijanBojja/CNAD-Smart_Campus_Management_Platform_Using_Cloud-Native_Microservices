server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: attendance-service

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:smart_campus_attendance}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:smartcampus_user}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

# JWT Security Configuration
jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000} # 24 hours

# Downstream Microservices Integration
auth-service:
  url: ${AUTH_SERVICE_URL:http://localhost:8081}
  connect-timeout-ms: ${AUTH_SERVICE_CONNECT_TIMEOUT_MS:5000}
  read-timeout-ms: ${AUTH_SERVICE_READ_TIMEOUT_MS:5000}

student-service:
  url: ${STUDENT_SERVICE_URL:http://localhost:8082}
  connect-timeout-ms: ${STUDENT_SERVICE_CONNECT_TIMEOUT_MS:5000}
  read-timeout-ms: ${STUDENT_SERVICE_READ_TIMEOUT_MS:5000}

academic-service:
  url: ${ACADEMIC_SERVICE_URL:http://localhost:8083}
  connect-timeout-ms: ${ACADEMIC_SERVICE_CONNECT_TIMEOUT_MS:5000}
  read-timeout-ms: ${ACADEMIC_SERVICE_READ_TIMEOUT_MS:5000}

# CORS Configuration
cors:
  allowed-origins: ${FRONTEND_URL:http://localhost:5173}

# OpenAPI Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true

logging:
  level:
    root: INFO
    com.smartcampus.attendance: DEBUG
    org.springframework.security: INFO
